// Definir la variable hora
let hora = 15;

// Usar condicionales para determinar el saludo
if (hora >= 6 && hora < 12) {
    console.log("Buenos días");
} else if (hora >= 12 && hora < 18) {
    console.log("Buenas tardes");
} else {
    console.log("Buenas noches");
}